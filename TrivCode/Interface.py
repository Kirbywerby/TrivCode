from http.server import ThreadingHTTPServer


class Interface:
    def __init__(self, trivcode: TrivCode)
    self.quiz = trivcode 
    self.widow = Tk()
    self.window.title("TrivCode")
    self.window.geometry("1000x1000")

    self.display_title()
    self.canvas = Canvas(width=1000, height = 1000)
    self.question_text = self.canvas.create_text(400,125, text = "Question", width = 680,)